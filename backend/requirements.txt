# Core framework
fastapi>=0.115
uvicorn[standard]>=0.34

# MongoDB
motor>=3.3
pymongo>=4.6

# Data validation
pydantic>=2.11
pydantic-settings>=2.9
email-validator>=2.2

# Authentication
python-jose[cryptography]>=3.3
bcrypt>=4.0

# Form parsing
python-multipart>=0.0.18

# LangChain
langchain-core>=0.3
langchain-community>=0.3
langchain-openai>=0.2           # used when LLM_PROVIDER=openai, and for Groq fallback (OpenAI-compatible)
langchain-anthropic>=0.2        # used when LLM_PROVIDER=anthropic
langchain-google-genai>=2.0     # used when LLM_PROVIDER=gemini

# Testing
pytest>=8.0
httpx>=0.27
pytest-asyncio>=0.23
